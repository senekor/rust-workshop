#![allow(unused)]

enum Weekday {
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday,
    Sunday,
}

fn main() {
    let day = Weekday::Monday;
    // match day {
    // }
}
